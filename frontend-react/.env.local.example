# Copy this file to .env.local and fill in the actual paths.
# These are NOT VITE_-prefixed — they are read server-side by vite.config.ts only
# and never exposed to the client bundle.

SSL_KEY_PATH=/path/to/your/dev.key
SSL_CERT_PATH=/path/to/your/dev.crt
