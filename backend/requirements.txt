# Voxyflow Backend Dependencies

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# Settings
pydantic-settings>=2.0.0

# HTTP client (Claude API, remote TTS)
httpx>=0.27.0

# TTS (optional — uncomment when ready)
# sherpa-onnx>=1.10.0

# STT fallback (optional — uncomment when ready)
# faster-whisper>=1.0.0

# Secure key storage
keyring>=25.0.0
keyrings.alt>=5.0.0

# RAG / ChromaDB (vector store + embeddings)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# File upload support
python-multipart>=0.0.9

# Document parsers (Phase 2)
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0

# Scheduler (background jobs + heartbeat)
apscheduler>=3.10.0

# Dev
python-dotenv>=1.0.0
openai>=1.0.0
python-multipart>=0.0.9
